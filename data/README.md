# Dados locais

O arquivo real `db.json` não é versionado. Para desenvolvimento local, copie `db.example.json` para `db.json`. Em produção, prefira definir `AXB_DB_PATH` apontando para um caminho fora do diretório público do servidor.
